#usda 1.0
(
    doc = """"""
    defaultPrim = "campfire"
)

def "campfire"
(
    variants = {
            string source = "geomMatInstances"
        }
        prepend variantSets = "source"
)
{
    variantSet "source" = {
        "geomMatInstances" (
            prepend payload = [@./geomMatInstances/source_geomMatInstances.usda@]
        )
        {
        }
    }
}