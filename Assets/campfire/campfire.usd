#usda 1.0
(
    doc = """Main entry point for an asset"""
    metersPerUnit = 0.01
    upAxis = "Y"
)

def "campfire" (
    assetInfo = {
        string name = "campfire"
    }
    kind = "component"
    append references = [
        @./contrib/tag/tag.usda@,
        @./contrib/material/material.usda@,
        @./contrib/geometry/geometry.usda@,
    ]
)
{
}